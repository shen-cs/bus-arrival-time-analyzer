import React, { Component } from 'react';

export default class extends Component {
	render() {
		const styles = {
			text: {
				margin:0,
			},
		};
		// const { text } = this.props;
		return (
			
			<text>lala</text>
		)
	}
}